<%- include('partials/header') %>

<section class="pricing-hero">
  <div class="pricing-hero-inner">
    <h1 class="pricing-title">Choose your Dream X experience</h1>
    <p class="pricing-subtitle">Start free. Upgrade when you’re ready to grow.</p>
  </div>
</section>

<section class="pricing-grid-section">
  <div class="pricing-grid">
    <% tiers.forEach(function(t){ %>
      <div class="pricing-card <%= t.highlight ? 'pricing-highlight' : '' %>">
        <div class="pricing-card-header">
          <h2 class="tier-name"><%= t.name %></h2>
          <p class="tier-tagline"><%= t.tagline %></p>
          <div class="tier-price"><%= t.price %></div>
        </div>
        <ul class="tier-features">
          <% t.features.forEach(function(f){ %>
            <li><%= f %></li>
          <% }); %>
        </ul>
        <div class="tier-actions">
          <a href="#" class="tier-btn <%= t.highlight ? 'primary' : 'outline' %>">Get Started</a>
        </div>
      </div>
    <% }); %>
  </div>
</section>

<%- include('partials/footer') %>
