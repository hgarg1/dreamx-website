<%- include('partials/header') %>

<main class="onboarding-empty-page">
    <style>
        .onboarding-empty-page {
            position: relative;
            min-height: 100vh;
            padding: 5rem 1.5rem 4rem;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
            background: linear-gradient(135deg, #a8edea 0%, #fed6e3 50%, #fbc2eb 100%);
            background-size: 400% 400%;
            animation: gradientShift 15s ease infinite;
        }

        @keyframes gradientShift {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .onboarding-empty-card {
            position: relative;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 1.25rem;
            padding: 2.5rem 2rem 2rem;
            width: min(960px, 100%);
            border-radius: 28px;
            overflow: hidden;
            box-shadow: 0 20px 70px rgba(0, 0, 0, 0.15);
            backdrop-filter: blur(12px);
            background: rgba(255, 255, 255, 0.12);
            border: 1px solid rgba(255, 255, 255, 0.2);
            z-index: 2;
        }

        /* Floating particles */
        .onboarding-empty-page .particle {
            position: absolute;
            border-radius: 50%;
            pointer-events: none;
            animation: float 20s infinite ease-in-out;
            opacity: 0.6;
            z-index: 1;
        }

        @keyframes float {
            0%, 100% { transform: translate(0, 0) rotate(0deg); }
            33% { transform: translate(30px, -30px) rotate(120deg); }
            66% { transform: translate(-20px, 20px) rotate(240deg); }
        }

        /* Glowing orbs that follow mouse */
        .cursor-glow {
            position: fixed;
            width: 300px;
            height: 300px;
            border-radius: 50%;
            background: radial-gradient(circle, rgba(168, 237, 234, 0.3) 0%, transparent 70%);
            pointer-events: none;
            transform: translate(-50%, -50%);
            transition: all 0.15s ease-out;
            z-index: 1;
        }

        .cursor-glow-2 {
            width: 200px;
            height: 200px;
            background: radial-gradient(circle, rgba(251, 194, 235, 0.3) 0%, transparent 70%);
            transition: all 0.25s ease-out;
        }

        /* Main icon */
        .welcome-icon {
            font-size: clamp(5rem, 12vw, 8rem);
            animation: bounce 2s infinite;
            margin-bottom: 0.5rem;
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        .error-title {
            font-size: clamp(1.8rem, 4vw, 2.6rem);
            font-weight: 800;
            color: #fff;
            margin: 0;
            text-align: center;
            text-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
            animation: fadeInUp 0.8s ease-out;
        }

        .error-message {
            font-size: clamp(1rem, 2vw, 1.2rem);
            color: rgba(255, 255, 255, 0.92);
            text-align: center;
            margin: 0;
            max-width: 640px;
            text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
            animation: fadeInUp 1s ease-out;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Floating emoji decorations */
        .floating-emoji {
            position: absolute;
            font-size: 3rem;
            animation: floatEmoji 4s infinite ease-in-out;
            user-select: none;
            transition: transform 0.3s ease;
            z-index: 2;
        }

        .floating-emoji:hover {
            transform: scale(1.3) rotate(20deg);
            cursor: pointer;
        }

        @keyframes floatEmoji {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(10deg); }
        }

        .emoji-1 { top: 12%; left: 6%; animation-delay: 0s; }
        .emoji-2 { top: 22%; right: 10%; animation-delay: 0.5s; }
        .emoji-3 { bottom: 18%; left: 12%; animation-delay: 1s; }
        .emoji-4 { bottom: 26%; right: 8%; animation-delay: 1.5s; }
        .emoji-5 { top: 48%; left: 2%; animation-delay: 2s; }
        .emoji-6 { top: 55%; right: 6%; animation-delay: 2.5s; }

        /* Buttons */
        .button-group {
            display: flex;
            gap: 1.5rem;
            flex-wrap: wrap;
            justify-content: center;
            animation: fadeInUp 1.2s ease-out;
            margin-top: 0.5rem;
        }

        .button-group form {
            margin: 0;
        }

        .btn-hero {
            padding: 1rem 2.5rem;
            font-size: 1.05rem;
            font-weight: 800;
            text-decoration: none;
            border-radius: 50px;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            border: none;
            cursor: pointer;
            position: relative;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.28);
            transform: translateY(0);
            color: #1f2937;
        }

        .btn-hero::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }

        .btn-hero:hover::before {
            width: 300px;
            height: 300px;
        }

        .btn-hero:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.35);
        }

        .btn-hero:active {
            transform: translateY(-2px);
        }

        .btn-primary-hero {
            background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%);
        }

        .btn-secondary-hero {
            background: linear-gradient(135deg, #fbc2eb 0%, #a6c1ee 100%);
            color: #fff;
        }

        /* Interactive grid background */
        .grid-background {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image:
                linear-gradient(rgba(255, 255, 255, 0.05) 1px, transparent 1px),
                linear-gradient(90deg, rgba(255, 255, 255, 0.05) 1px, transparent 1px);
            background-size: 50px 50px;
            animation: gridMove 20s linear infinite;
            z-index: 0;
        }

        @keyframes gridMove {
            0% { transform: translate(0, 0); }
            100% { transform: translate(50px, 50px); }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .floating-emoji { display: none; }
            .onboarding-empty-card { padding: 2rem 1.5rem; }
            .button-group { flex-direction: column; width: 100%; max-width: 320px; }
            .btn-hero { width: 100%; text-align: center; }
        }

        /* Sparkles */
        .sparkle {
            position: absolute;
            width: 4px;
            height: 4px;
            background: white;
            border-radius: 50%;
            animation: sparkle 2s infinite ease-in-out;
            pointer-events: none;
            z-index: 2;
        }

        @keyframes sparkle {
            0%, 100% { opacity: 0; transform: scale(0); }
            50% { opacity: 1; transform: scale(1); }
        }

        /* Features list */
        .features {
            display: flex;
            gap: 1rem;
            margin: 0 0 1rem;
            flex-wrap: wrap;
            justify-content: center;
            animation: fadeInUp 1.1s ease-out;
        }

        .feature {
            background: rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(10px);
            padding: 1rem 1.5rem;
            border-radius: 16px;
            color: #fff;
            font-weight: 700;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.2);
        }
    </style>

    <div class="grid-background"></div>
    <div class="cursor-glow" id="cursorGlow1"></div>
    <div class="cursor-glow cursor-glow-2" id="cursorGlow2"></div>

    <div class="onboarding-empty-card">
        <!-- Floating Emojis -->
        <div class="floating-emoji emoji-1">🎯</div>
        <div class="floating-emoji emoji-2">🚀</div>
        <div class="floating-emoji emoji-3">💡</div>
        <div class="floating-emoji emoji-4">⭐</div>
        <div class="floating-emoji emoji-5">🎨</div>
        <div class="floating-emoji emoji-6">✨</div>

        <!-- Main Content -->
        <div class="welcome-icon">👋</div>
        <h2 class="error-title">Welcome to Dream X!</h2>
        <p class="error-message">
            You haven't set up your profile yet. Let's get started and build your dream community together!
        </p>

        <!-- Features -->
        <div class="features">
            <div class="feature">📸 Share your passion</div>
            <div class="feature">💬 Connect with others</div>
            <div class="feature">🎯 Achieve your goals</div>
        </div>

        <!-- Action Buttons -->
        <div class="button-group">
            <form action="/onboarding/start" method="POST">
                <button type="submit" class="btn-hero btn-primary-hero">
                    <span>✨ Start Onboarding</span>
                </button>
            </form>
            <a href="/feed" class="btn-hero btn-secondary-hero">
                <span>Explore Feed 🏠</span>
            </a>
        </div>
    </div>
</main>

<script>
    // Cursor glow effect
    const glow1 = document.getElementById('cursorGlow1');
    const glow2 = document.getElementById('cursorGlow2');

    document.addEventListener('mousemove', (e) => {
        glow1.style.left = e.clientX + 'px';
        glow1.style.top = e.clientY + 'px';

        // Second glow with delay
        setTimeout(() => {
            glow2.style.left = e.clientX + 'px';
            glow2.style.top = e.clientY + 'px';
        }, 100);
    });

    // Create floating particles
    function createParticles() {
        const colors = ['#a8edea', '#fed6e3', '#fbc2eb', '#a6c1ee', '#ffd700'];
        for (let i = 0; i < 20; i++) {
            const particle = document.createElement('div');
            particle.className = 'particle';
            particle.style.width = Math.random() * 60 + 20 + 'px';
            particle.style.height = particle.style.width;
            particle.style.left = Math.random() * 100 + '%';
            particle.style.top = Math.random() * 100 + '%';
            particle.style.background = colors[Math.floor(Math.random() * colors.length)];
            particle.style.animationDelay = Math.random() * 5 + 's';
            particle.style.animationDuration = Math.random() * 10 + 15 + 's';
            document.querySelector('.onboarding-empty-page').appendChild(particle);
        }
    }

    // Create sparkles on click
    document.addEventListener('click', (e) => {
        for (let i = 0; i < 5; i++) {
            const sparkle = document.createElement('div');
            sparkle.className = 'sparkle';
            sparkle.style.left = e.clientX + (Math.random() - 0.5) * 50 + 'px';
            sparkle.style.top = e.clientY + (Math.random() - 0.5) * 50 + 'px';
            sparkle.style.animationDelay = i * 0.1 + 's';
            document.querySelector('.onboarding-empty-page').appendChild(sparkle);

            setTimeout(() => sparkle.remove(), 2000);
        }
    });

    // Initialize
    createParticles();

    // Emoji interaction - make them react to proximity
    const emojis = document.querySelectorAll('.floating-emoji');
    document.addEventListener('mousemove', (e) => {
        emojis.forEach(emoji => {
            const rect = emoji.getBoundingClientRect();
            const emojiX = rect.left + rect.width / 2;
            const emojiY = rect.top + rect.height / 2;
            const distance = Math.sqrt(
                Math.pow(e.clientX - emojiX, 2) +
                Math.pow(e.clientY - emojiY, 2)
            );

            if (distance < 150) {
                const angle = Math.atan2(e.clientY - emojiY, e.clientX - emojiX);
                const push = (150 - distance) / 10;
                emoji.style.transform = `translate(${-Math.cos(angle) * push}px, ${-Math.sin(angle) * push}px) scale(1.1)`;
            } else {
                emoji.style.transform = '';
            }
        });
    });
</script>

<%- include('partials/footer') %>
